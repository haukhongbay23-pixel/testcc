-- Xóa menu cũ để không bị chồng chéo
if game.CoreGui:FindFirstChild("Orion") then
    game.CoreGui:FindFirstChild("Orion"):Destroy()
end
if game.CoreGui:FindFirstChild("ChiiHauuToggle") then
    game.CoreGui:FindFirstChild("ChiiHauuToggle"):Destroy()
end

local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({
    Name = "Apple Hub Clone | chii Hauu Edition", 
    HidePremium = false, 
    SaveConfig = true,
    IntroText = "Welcome chii Hauu",
    ConfigFolder = "ChiiHauuConfig"
})

-- // NÚT BẬT/TẮT MENU (TOGGLE BUTTON) //
local ScreenGui = Instance.new("ScreenGui")
local ToggleButton = Instance.new("TextButton")
ScreenGui.Name = "ChiiHauuToggle"
ScreenGui.Parent = game.CoreGui
ToggleButton.Name = "ToggleButton"
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
ToggleButton.Position = UDim2.new(0, 10, 0, 150)
ToggleButton.Size = UDim2.new(0, 50, 0, 50)
ToggleButton.Text = "Hauu"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Draggable = true
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 10)
Corner.Parent = ToggleButton

ToggleButton.MouseButton1Click:Connect(function()
    local orionGui = game.CoreGui:FindFirstChild("Orion")
    if orionGui then
        orionGui.Enabled = not orionGui.Enabled
    end
end)

-- // CẤU HÌNH //
_G.AutoFarm = false
_G.Weapon = "Melee" 
_G.DistanceMob = 10

-- // DỮ LIỆU QUEST //
local function GetQuest()
    local lvl = game.Players.LocalPlayer.Data.Level.Value
    if lvl >= 1 and lvl <= 9 then
        return {"BanditQuest1", "Bandit", CFrame.new(1059, 15, 1550)}
    elseif lvl >= 10 and lvl <= 14 then
        return {"MonkeyQuest1", "Monkey", CFrame.new(-1598, 35, 153)}
    elseif lvl >= 15 and lvl <= 29 then
        return {"GorillaQuest1", "Gorilla", CFrame.new(-1598, 35, 153)}
    else
        return {"BanditQuest1", "Bandit", CFrame.new(1059, 15, 1550)}
    end
end

-- // HÀM HỖ TRỢ //
function EquipWeapon()
    pcall(function()
        for i,v in pairs(game.Players.LocalPlayer.Backpack:GetChildren()) do
            if v.ToolTip == _G.Weapon or v.Name == _G.Weapon then
                game.Players.LocalPlayer.Character.Humanoid:EquipTool(v)
            end
        end
    end)
end

function TweenTo(Pos)
    local char = game.Players.LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        local Distance = (Pos.Position - char.HumanoidRootPart.Position).Magnitude
        local Speed = 300 
        local tween = game:GetService("TweenService"):Create(char.HumanoidRootPart, TweenInfo.new(Distance/Speed, Enum.EasingStyle.Linear), {CFrame = Pos})
        tween:Play()
        return tween
    end
end

-- // LOGIC CHÍNH //
spawn(function()
    while wait() do
        if _G.AutoFarm then
            pcall(function()
                if not game:GetService("Players").LocalPlayer.PlayerGui.Main:FindFirstChild("Quest") then
                    local QuestData = GetQuest()
                    TweenTo(QuestData[3])
                    if (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - QuestData[3].Position).Magnitude < 10 then
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StartQuest", QuestData[1], 1)
                    end
                else
                    local QuestData = GetQuest()
                    local Enemy = game.Workspace.Enemies:FindFirstChild(QuestData[2]) or game.Workspace.Enemies:FindFirstChildOfClass("Model")
                    if Enemy and Enemy:FindFirstChild("HumanoidRootPart") and Enemy.Humanoid.Health > 0 then
                        EquipWeapon()
                        TweenTo(Enemy.HumanoidRootPart.CFrame * CFrame.new(0, _G.DistanceMob, 0))
                        game:GetService('VirtualUser'):CaptureController()
                        game:GetService('VirtualUser'):ClickButton1(Vector2.new(851, 158), game.Workspace.CurrentCamera.CFrame)
                    else
                        TweenTo(GetQuest()[3] * CFrame.new(0, 50, 0))
                    end
                end
            end)
        end
    end
end)

-- // GIAO DIỆN //
local MainTab = Window:MakeTab({Name = "Main", Icon = "rbxassetid://4483345998"})
MainTab:AddLabel("User: chii Hauu")

MainTab:AddDropdown({
	Name = "Chọn Vũ Khí",
	Default = "Melee",
	Options = {"Melee", "Sword", "Blox Fruit"},
	Callback = function(Value)
		_G.Weapon = Value
	end    
})

MainTab:AddToggle({
	Name = "Bật Auto Farm Level",
	Default = false,
	Callback = function(Value)
		_G.AutoFarm = Value
	end    
})

MainTab:AddSlider({
	Name = "Khoảng cách Farm (Y)",
	Min = 5,
	Max = 25,
	Default = 10,
	Color = Color3.fromRGB(255, 0, 0),
	Increment = 1,
	ValueName = "Studs",
	Callback = function(Value)
		_G.DistanceMob = Value
	end    
})

OrionLib:Init()
